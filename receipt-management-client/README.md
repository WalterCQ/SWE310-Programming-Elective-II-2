# Receipt Management Client

Vite React frontend for the Receipt Management Application.

## Scripts

```bash
npm install
npm run dev
npm run build
npm run lint
```

Set `VITE_API_BASE_URL` in `.env` when the backend is not running at `http://localhost:5068/api`.
